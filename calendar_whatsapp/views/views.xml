<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="calendar_whatsapp.list">
      <field name="name">calendar_whatsapp list</field>
      <field name="model">calendar_whatsapp.calendar_whatsapp</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="calendar_whatsapp.action_window">
      <field name="name">calendar_whatsapp window</field>
      <field name="res_model">calendar_whatsapp.calendar_whatsapp</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="calendar_whatsapp.action_server">
      <field name="name">calendar_whatsapp server</field>
      <field name="model_id" ref="model_calendar_whatsapp_calendar_whatsapp"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="calendar_whatsapp" id="calendar_whatsapp.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="calendar_whatsapp.menu_1" parent="calendar_whatsapp.menu_root"/>
    <menuitem name="Menu 2" id="calendar_whatsapp.menu_2" parent="calendar_whatsapp.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="calendar_whatsapp.menu_1_list" parent="calendar_whatsapp.menu_1"
              action="calendar_whatsapp.action_window"/>
    <menuitem name="Server to list" id="calendar_whatsapp" parent="calendar_whatsapp.menu_2"
              action="calendar_whatsapp.action_server"/>
    -->
  </data>
</openerp>